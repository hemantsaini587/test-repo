pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Repository already checked out successfully"
                dir('.') {
                    bat 'dir'
                }
            }
        }
    }
}
